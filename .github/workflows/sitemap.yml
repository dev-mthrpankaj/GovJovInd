name: Generate Sitemap

on:
  push:
    branches:
      - main
    paths-ignore:
      - sitemap.xml
      - sitemap-*.xml
      - data/sitemap-content-signatures.json
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: sitemap-main
  cancel-in-progress: true

jobs:
  generate-sitemap:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository history
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Generate sitemap index and category sitemaps
        run: node scripts/generate-sitemap.js

      - name: Commit sitemaps when changed
        run: |
          git add sitemap.xml sitemap-*.xml data/sitemap-content-signatures.json

          if git diff --cached --quiet; then
            echo "No sitemap changes to commit."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Update sitemaps"
          git push
