(function () {
    "use strict";

    const helpers = window.GJU_QUIZ_REGISTRY_HELPERS;
    if (!helpers) return;

    const { registerSubjectQuizzes } = helpers;

    registerSubjectQuizzes([]);
}());
